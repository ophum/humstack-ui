export enum NetworkV0Annotation {
    NetworkType = "networkv0/network_type",
    DefaultGateway = "networkv0/default_gateway",
}

export enum NetworkV0NetworkType {
    Bridge = "Bridge",
}

export enum BlockStorageV0Annotation {
    Type = "blockstoragev0/type",
    NodeName = "blockstoragev0/node_name",
}

export enum BlockStorageV0Type {
    Local = "Local",
}

export enum VirtualMachineV0Annotation {
    NodeName = "virtualmachinev0/node_name",
}

export enum VirtualRouterV0Annotation {
    NodeName = "virtualrouterv0/node_name",
}
